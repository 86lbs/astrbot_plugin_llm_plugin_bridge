{
  "allow_execute": {
    "type": "bool",
    "description": "是否允许 LLM 通过工具执行插件指令",
    "default": true
  },
  "execute_require_admin": {
    "type": "bool",
    "description": "LLM 执行指令时是否需要用户具有管理员权限",
    "default": false
  },
  "blocked_commands": {
    "type": "list",
    "description": "禁止 LLM 执行的指令名称列表",
    "default": [],
    "items": {
      "type": "string"
    }
  },
  "list_mode": {
    "type": "string",
    "description": "指令列表过滤模式",
    "default": "all",
    "options": ["all", "whitelist", "blacklist"],
    "labels": ["全部", "白名单", "黑名单"]
  },
  "command_whitelist": {
    "type": "list",
    "description": "白名单模式下显示的指令",
    "default": [],
    "items": {
      "type": "string"
    }
  },
  "command_blacklist": {
    "type": "list",
    "description": "黑名单模式下隐藏的指令",
    "default": [],
    "items": {
      "type": "string"
    }
  },
  "hide_plugin_info": {
    "type": "bool",
    "description": "在指令列表中隐藏指令所属的插件名称",
    "default": false
  },
  "show_wake_prefix_in_list": {
    "type": "bool",
    "description": "在指令列表中显示完整的唤醒词前缀",
    "default": true
  },
  "enable_tool_logging": {
    "type": "bool",
    "description": "是否记录 LLM Tool 的调用和响应事件",
    "default": true
  },
  "log_level": {
    "type": "string",
    "description": "工具调用日志的输出级别",
    "default": "info",
    "options": ["info", "debug"],
    "labels": ["信息", "调试"]
  },
  "max_history_per_session": {
    "type": "int",
    "description": "每个会话保留的最大消息数",
    "default": 60
  },
  "session_expire_seconds": {
    "type": "int",
    "description": "会话过期时间（秒），超时无活动的会话将被清理",
    "default": 1800
  },
  "intent_time_window": {
    "type": "float",
    "description": "意图检测时间窗口（秒），用于判断用户是否已通过指令触发功能",
    "default": 5.0
  },
  "max_invocation_records": {
    "type": "int",
    "description": "最大指令调用记录数",
    "default": 50
  },
  "cleanup_threshold": {
    "type": "int",
    "description": "清理阈值，每多少次操作触发一次过期会话清理",
    "default": 100
  }
}
